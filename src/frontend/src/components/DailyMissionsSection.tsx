// Missions are now handled inside TrainerHub
// This file is kept for backwards compatibility
export function DailyMissionsSection(_props: {
  isLoggedIn: boolean;
  onLoginClick: () => void;
}) {
  return null;
}
